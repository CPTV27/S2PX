# ── S2PX CI/CD Pipeline ──
# Runs on every push/PR to main: type check → build → unit tests → E2E tests.
# Playwright runs both Desktop Chrome (PM Dashboard, Revenue, Scorecard, Navigation)
# and iPhone 13 mobile (Scantech field ops) projects.

name: S2PX CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # ── Fail-fast gates (cheapest checks first) ──

      - name: Type Check
        run: npx tsc --noEmit

      - name: Build Vite App
        run: npx vite build

      - name: Run Unit Tests (Vitest)
        run: npx vitest run

      # ── E2E Tests (Playwright) ──

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E Tests (Playwright)
        run: npx playwright test
        env:
          # Dummy Firebase config so the SDK initializes without crashing.
          # E2E tests bypass Firebase auth via window.__E2E_AUTH_USER__ injection —
          # no real Firebase calls are made.
          VITE_FIREBASE_API_KEY: ci-placeholder-key
          VITE_FIREBASE_AUTH_DOMAIN: ci-placeholder.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: ci-placeholder
          VITE_FIREBASE_STORAGE_BUCKET: ci-placeholder.appspot.com
          VITE_FIREBASE_MESSAGING_SENDER_ID: '000000000000'
          VITE_FIREBASE_APP_ID: '1:000000000000:web:0000000000000000'

      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Upload Playwright Traces
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-traces
          path: test-results/
          retention-days: 7
